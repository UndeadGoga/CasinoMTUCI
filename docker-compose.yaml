version: '3.9'
services:

  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: 'postgres'
      POSTGRES_DB: 'details'
    ports:
      - 5432:5432
    volumes:
      - ~volumes/pg-data:/var/lib/postgresql/data
